一般に、コンピューターでは、1秒間に10^8 ~ 10^9回程度計算できる.
例えば競技プログラミングだと1秒~2秒以内で答えを出さなければならないので 
およそ 2億 ～ 10億通り程度しか探索できないことが多い

2億:二秒あれば間に合うことが多い
5億:複雑な処理だと間に合わない
10億:非常に単純でなければ間に合わない

ctl k v マークダウンプレビューマークダウンプレビュー

setのin演算はハッシュをつかうのでO(1)
dictもハッシュ

pythonでは大体2秒以内に10^6回程度の計算ができる　疑問？

listとdeque

listは一番最初や途中に要素を追加・削除する動きが遅い
一方でi番目の要素を特定するのは早い

dequeは一番最初や最後に要素を追加・削除する動きが速い

一般的な競技プログラミングでは、10^5～10^7個の要素が現実的な上限

heap:二分木のデータ構造
1.ヒープの構築 o(N)
2.最大値・最小値を参照 o(1)
3.要素を追加削除 o(logN)
（heapify up）：最後尾に追加してから、親ノードと比較しながら上に上げていく
（heapify down）：先頭（最大値または最小値）を削除し、末尾の要素を根に置いて下に下げる